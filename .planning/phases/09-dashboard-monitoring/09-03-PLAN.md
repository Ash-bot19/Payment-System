---
phase: 09-dashboard-monitoring
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - payment-backend/tests/e2e/test_dashboard_e2e.py
autonomous: false
requirements:
  - DASH-E2E
  - DASH-UAT

must_haves:
  truths:
    - "Streamlit dashboard is accessible at http://localhost:8501 when Docker stack is running"
    - "All 4 dashboard pages render data after seed + dbt run"
    - "Grafana at localhost:3000 shows 2 provisioned alert rules"
    - "Prometheus at localhost:9090 lists both webhook-service and ml-scoring-service targets"
  artifacts:
    - path: "payment-backend/tests/e2e/test_dashboard_e2e.py"
      provides: "E2E tests for Streamlit dashboard and monitoring stack"
      min_lines: 40
  key_links:
    - from: "payment-backend/tests/e2e/test_dashboard_e2e.py"
      to: "http://localhost:8501"
      via: "HTTP GET to Streamlit health endpoint"
      pattern: "localhost:8501"
    - from: "payment-backend/tests/e2e/test_dashboard_e2e.py"
      to: "http://localhost:9090"
      via: "HTTP GET to Prometheus targets API"
      pattern: "localhost:9090"
---

<objective>
E2E tests validating the full dashboard + monitoring stack, plus a human verification checkpoint for visual dashboard and Grafana review.

Purpose: Automated E2E confirms services are reachable and configured correctly. Human checkpoint validates visual rendering and UX.
Output: E2E test file, human verification of dashboard and Grafana.
</objective>

<execution_context>
@D:/Github/Payment System/.claude/get-shit-done/workflows/execute-plan.md
@D:/Github/Payment System/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dashboard-monitoring/09-CONTEXT.md
@.planning/phases/09-dashboard-monitoring/09-01-SUMMARY.md
@.planning/phases/09-dashboard-monitoring/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E tests for dashboard and monitoring stack</name>
  <files>payment-backend/tests/e2e/test_dashboard_e2e.py</files>
  <read_first>
    payment-backend/tests/e2e/test_phase8_e2e.py,
    payment-backend/infra/docker-compose.yml,
    payment-backend/monitoring/grafana/provisioning/alerting/payment_alerts.json,
    payment-backend/monitoring/grafana/provisioning/datasources/prometheus.json,
    payment-backend/monitoring/prometheus/prometheus.yml
  </read_first>
  <action>
Create `payment-backend/tests/e2e/test_dashboard_e2e.py`.

All tests should gracefully skip if the services are not running (use `urllib.request.urlopen` with timeout and skip on `ConnectionError`/`URLError`).

Import pattern:
```python
import json
import os
import urllib.request
import urllib.error
import pytest
import structlog

logger = structlog.get_logger(__name__)
```

Helper function:
```python
def _fetch(url: str, timeout: int = 5) -> str:
    """Fetch URL content. Raises on failure."""
    req = urllib.request.Request(url)
    with urllib.request.urlopen(req, timeout=timeout) as resp:
        return resp.read().decode()
```

Skip fixture:
```python
@pytest.fixture(autouse=True)
def _skip_without_stack():
    """Skip all tests if Docker stack is not running."""
    try:
        _fetch("http://localhost:8501/_stcore/health", timeout=3)
    except (urllib.error.URLError, ConnectionError, OSError):
        pytest.skip("Docker stack not running (streamlit-dashboard not reachable at :8501)")
```

**Test 1: `test_streamlit_health`:**
- `_fetch("http://localhost:8501/_stcore/health")` should return `"ok"` (Streamlit health endpoint)

**Test 2: `test_streamlit_main_page_loads`:**
- `_fetch("http://localhost:8501/")` should return HTML containing `Payment System Dashboard` (the page title set in `st.set_page_config`)

**Test 3: `test_prometheus_targets_include_ml_service`:**
- `_fetch("http://localhost:9090/api/v1/targets")` -- parse JSON
- Extract `data.activeTargets` list
- Assert at least one target has `labels.job == "ml-scoring-service"`
- Assert at least one target has `labels.job == "webhook-service"`
- If Prometheus not reachable, skip test

**Test 4: `test_prometheus_ml_service_metrics_available`:**
- `_fetch("http://localhost:9090/api/v1/query?query=up{job='ml-scoring-service'}")` -- parse JSON
- Assert `data.result` is non-empty (ml-scoring-service is being scraped)
- Skip if Prometheus not reachable

**Test 5: `test_grafana_datasource_provisioned`:**
- `_fetch("http://localhost:3000/api/datasources")` with basic auth header (`admin:admin`) -- `urllib.request.Request` with `Authorization: Basic YWRtaW46YWRtaW4=`
- Parse JSON, assert at least one datasource with `type == "prometheus"` and `uid == "prometheus"`
- Skip if Grafana not reachable

**Test 6: `test_grafana_alert_rules_provisioned`:**
- `_fetch("http://localhost:3000/api/v1/provisioning/alert-rules")` with basic auth header
- Parse JSON (list of alert rule objects)
- Assert at least 2 rules exist
- Assert one has `title == "WebhookErrorRate"`
- Assert one has `title == "MLScoringLatencyP99"`
- Skip if Grafana not reachable

All tests use structlog, not print. Each test is independent (no ordering dependency).
  </action>
  <verify>
    <automated>cd payment-backend && python -c "import ast; ast.parse(open('tests/e2e/test_dashboard_e2e.py').read()); print('Syntax OK')" && python -m pytest tests/e2e/test_dashboard_e2e.py -x -q --tb=short 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/test_dashboard_e2e.py` exists and parses without syntax errors
    - `grep "test_streamlit_health" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "test_streamlit_main_page_loads" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "test_prometheus_targets_include_ml_service" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "test_grafana_datasource_provisioned" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "test_grafana_alert_rules_provisioned" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "WebhookErrorRate" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "MLScoringLatencyP99" tests/e2e/test_dashboard_e2e.py` returns a match
    - `grep "localhost:8501" tests/e2e/test_dashboard_e2e.py` returns at least 1 match
    - `grep "localhost:9090" tests/e2e/test_dashboard_e2e.py` returns at least 1 match
    - `grep "localhost:3000" tests/e2e/test_dashboard_e2e.py` returns at least 1 match
    - File does not contain `print(`
  </acceptance_criteria>
  <done>6 E2E tests covering Streamlit health, page rendering, Prometheus targets, Grafana datasource, and Grafana alert rules. All skip gracefully without live stack.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of dashboard and monitoring</name>
  <files>N/A</files>
  <action>
Human verifies the full dashboard and monitoring stack visually. This is a checkpoint -- no code changes. The executor presents the how-to-verify steps and waits for user approval.
  </action>
  <what-built>
    Complete Streamlit dashboard (4 pages with KPI cards, charts, filterable tables) connected to PostgreSQL dbt mart tables. Prometheus scraping ml-scoring-service. Grafana with 2 provisioned alert rules (WebhookErrorRate, MLScoringLatencyP99).
  </what-built>
  <how-to-verify>
    1. Ensure Docker stack is running: `cd payment-backend/infra && docker compose up -d`
    2. Run Alembic migrations: `cd payment-backend && PYTHONPATH=. DATABASE_URL_SYNC=postgresql://payment:payment@localhost:5432/payment_db alembic -c db/alembic.ini upgrade head`
    3. Run seed script: `cd payment-backend && DATABASE_URL=postgresql://payment:payment@localhost:5432/payment_db python scripts/seed_demo_data.py`
    4. Run dbt: `cd payment-backend && dbt run --project-dir dbt --profiles-dir dbt`
    5. Open http://localhost:8501 -- verify:
       - 4 pages visible in sidebar (Fraud Metrics, Payment Volume, Merchant Performance, Reconciliation)
       - Each page shows KPI metric cards with non-zero values
       - Each page shows a chart with data
       - Each page has a filter dropdown and data table
    6. Open http://localhost:9090/targets -- verify:
       - `webhook-service:8000` shows UP
       - `ml-scoring-service:8001` shows UP
    7. Open http://localhost:3000 (login: admin/admin) -- verify:
       - Alerting > Alert Rules shows WebhookErrorRate and MLScoringLatencyP99
       - Data Sources shows Prometheus pointing to prometheus:9090
    8. Run E2E tests: `cd payment-backend && python -m pytest tests/e2e/test_dashboard_e2e.py -v`
  </how-to-verify>
  <verify>Human confirms all items in how-to-verify pass</verify>
  <done>User types "approved" confirming dashboard renders data on all 4 pages, Prometheus targets are UP, and Grafana has both alert rules.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- E2E tests parse: `python -c "import ast; ast.parse(open('payment-backend/tests/e2e/test_dashboard_e2e.py').read())"`
- E2E tests run (skip if no stack): `cd payment-backend && python -m pytest tests/e2e/test_dashboard_e2e.py -x -q`
- All existing tests still pass: `cd payment-backend && python -m pytest tests/unit/ -x -q`
- Human visually confirms dashboard pages render with data
</verification>

<success_criteria>
E2E tests validate Streamlit, Prometheus, and Grafana are reachable and correctly configured. Human confirms all 4 dashboard pages display data, Prometheus shows both targets UP, and Grafana has 2 alert rules provisioned.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-monitoring/09-03-SUMMARY.md`
</output>
